// Add this to the existing types file
// Update the RadicalForm interface to ensure toString is properly typed

export interface RadicalForm {
  coefficient: number;
  radicand: number;
  numerator?: RadicalTerm;
  denominator?: RadicalTerm;
  isImaginary: boolean;
  toString: () => string;
}
